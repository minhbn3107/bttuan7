<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Edit Product</title>
        <link rel="stylesheet" href="index.css" />
    </head>
    <body>
        <div class="content">
            <form method="POST" action="/edit/<%= product.ma_sp %>">
                <h2>Edit Product</h2>

                <div>
                    <label>Product Code: <%= product.ma_sp %></label>
                </div>

                <div>
                    <label for="ten_sp">Product Name:</label>
                    <input
                        type="text"
                        id="ten_sp"
                        name="ten_sp"
                        value="<%= product.ten_sp %>"
                        required
                    />
                </div>

                <div>
                    <label for="so_luong">Quantity:</label>
                    <input
                        type="number"
                        id="so_luong"
                        name="so_luong"
                        value="<%= product.so_luong %>"
                        required
                    />
                </div>

                <div>
                    <label>Current Image:</label>
                    <img
                        src="<%= product.image_url %>"
                        width="100"
                        alt="Current product image"
                    />
                </div>

                <div>
                    <button type="submit">Update Product</button>
                    <a href="/">Cancel</a>
                </div>
            </form>
        </div>
    </body>
</html>
